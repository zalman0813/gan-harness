# ADR Index

_Auto-generated by `.claude/skills/plan-workflow/scripts/finalize_adr.py` at `/finalize`._

Do not edit manually — overwritten on every `/finalize` run. To revise an
ADR, write a new one with `supersedes: [old_id]`; the index regenerates
next finalize.

_(empty — the first ADR will appear here when the planner proposes one and
`/finalize` promotes it to `accepted`)_
